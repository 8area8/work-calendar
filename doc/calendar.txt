chaque case est un objet qu'on peut

calendar.setMonth(monthNumber)
calendar.setYear(year)

calendar.setWorkerMode(workerName)
calendar.setDefaultMode()

calendar.month
calendar.year
calendar.activeDay

calendar.showDay(dayNumber)


backend.getMonth()
{
    1: {
        {name: Olivier, start: 12h20, end: 2h30},
    }
}
for ([day, workers] in month.items()) {
    month.setDay(day, workers)
}

pour  chaque semaine dans mois:
  pour chaque jour dans semaine:
    createNode()

{
  days: {
    1: [{name: max, start: "10h00"}]
  }
}

save - update create delete worker

<Day disabled/>
<Day number="day.dayNumber" morning-number="day.workers(morgning)"/>
